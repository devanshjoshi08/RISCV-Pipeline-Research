\documentclass[conference]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{url}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{siunitx}

\graphicspath{{figures/}}

\begin{document}

\title{Pipeline Depth and Branch Prediction Effectiveness on FPGA:\\A Controlled Comparison of 4- to 8-Stage RV32IM Processors}

\author{
\IEEEauthorblockN{Devansh Joshi}
\IEEEauthorblockA{Department of Electrical and Computer Engineering\\
Email: devanshjoshi08@gmail.com}
}

\maketitle

\begin{abstract}
Pipeline depth is a fundamental design parameter for FPGA-based processors, yet its interaction with branch prediction effectiveness has not been quantified under controlled conditions on LUT-based fabrics. This paper presents five RV32IM pipeline variants---4, 5, 6, 7, and 8 stages---on Xilinx Artix-7 in which all non-pipeline microarchitectural variables are held constant from shared RTL. Post-place-and-route synthesis with three placement seeds per variant provides frequency and area measurements with error bars. Seven benchmark workloads---including official EEMBC CoreMark and four Embench-IoT benchmarks---are evaluated across all five depths using cycle-accurate simulation with hardware performance counters, producing 35 validated CPI and misprediction data points. All five variants produce identical instruction counts on every benchmark, confirming functional correctness. The 6-stage design achieves the highest throughput (64.1\,MIPS on CoreMark) and a CoreMark/MHz score of 1.88, while the 4-stage achieves the highest per-cycle efficiency at 2.23\,CoreMark/MHz.
\end{abstract}

\begin{IEEEkeywords}
RISC-V, pipeline depth, FPGA, Artix-7, RV32IM, branch prediction, gshare, microarchitecture
\end{IEEEkeywords}

% ====================================================================
\section{Introduction}
% ====================================================================

The RISC-V instruction set architecture has seen broad adoption in academia and industry because of its open specification, modular extension mechanism, and clean encoding format \cite{waterman2019riscv}. Many RISC-V soft cores now target FPGAs, ranging from bit-serial execution engines \cite{serv2019} to application-class superscalar designs \cite{zaruba2019ariane}. However, when designers choose a pipeline organization for an FPGA-based processor, they face a fundamental question: how deep should the pipeline be? Published cores differ in ISA support, microarchitectural features, and optimization goals, making direct comparisons difficult. To our knowledge, no published study isolates pipeline depth as the sole independent variable while holding the branch predictor, multiply-divide unit, memory system, and forwarding philosophy constant on a common FPGA target.

On ASIC processes, deeper pipelines shorten the critical path delay at each stage, raising the achievable clock frequency \cite{hennessy2017computer}. The Intel Pentium~4 family carried this principle to 20 and later 31 stages \cite{sprangle2002increasing}. The cost is increased CPI from deeper branch misprediction penalties and additional pipeline register overhead. Hrishikesh et~al.\ showed that an optimal pipeline depth exists that maximizes throughput for a given technology node \cite{hrishikesh2002optimal}.

FPGAs present a different set of constraints. Logic is implemented in look-up tables (LUTs) rather than custom gates, routing delay often dominates cell delay, and hard blocks such as DSP48E1 slices impose fixed latencies \cite{xilinx_7series}. These properties shift the frequency-versus-CPI tradeoff relative to ASIC designs \cite{kuon2007measuring}. The principle that deeper pipelines degrade branch prediction effectiveness through increased update latency is well established in the ASIC literature \cite{gonzalez1999power, hrishikesh2002optimal}. However, the quantitative magnitude of this effect on FPGA fabric has not been measured under controlled conditions. This gap matters because FPGA designers make pipeline depth decisions based on ASIC-derived intuitions, but the frequency-versus-depth relationship on FPGAs differs in important ways: LUT delay is function-independent, routing delay dominates and is not eliminated by register insertion, and synchronous BRAM introduces fixed memory latencies. Whether a 7- or 8-stage pipeline that may be justified on ASIC is also beneficial on FPGA depends on fabric-specific tradeoff data that has not been available from prior controlled experiments.

This paper addresses these questions through a controlled experiment spanning five pipeline depths. We present RV32IM pipeline variants with 4, 5, 6, 7, and 8 stages, all sharing identical RTL for the ALU, multiply-divide unit, gshare branch predictor, CSR unit, register file, instruction cache, and data memory. We synthesize all five on a Xilinx Artix-7 XC7A35T with three placement seeds per variant to obtain frequency and area measurements with statistical error bars. We then run benchmark workloads on each variant using cycle-accurate simulation with hardware performance counters to obtain validated CPI, branch counts, and misprediction rates.

The contributions of this work are:
\begin{enumerate}
    \item A controlled experimental methodology in which pipeline depth is the sole independent variable across five design points (4 through 8 stages), with all other microarchitectural modules shared from a common RTL codebase (Section~\ref{sec:invariants}).
    \item Multi-seed post-place-and-route synthesis on Artix-7, providing $F_{\max}$ and area measurements with error bars that quantify placement-dependent variance and show a two-tier frequency structure: the 4- and 5-stage designs cluster near 75\,MHz, while the 6-, 7-, and 8-stage designs cluster near 117--120\,MHz (Section~\ref{sec:synth}).
    \item Validated CPI measurements across 35 workload-depth combinations (7 benchmarks $\times$ 5 depths), all with matching instruction counts, showing that each additional pipeline stage costs approximately 0.12 CPI on average. Official CoreMark/MHz scores range from 2.23 (4-stage) to 1.58 (8-stage) (Section~\ref{sec:model}).
    \item Measurements showing that the official EEMBC CoreMark benchmark produces 5.7\% more mispredictions on a 6-stage pipeline than on a 4-stage, and the Embench statemate benchmark shows a 10\% misprediction increase from 4-stage to 7/8-stage, despite identical predictor hardware in all cases (Section~\ref{sec:cpi}).
\end{enumerate}

% ====================================================================
\section{Background and Related Work}
% ====================================================================

\subsection{RISC-V ISA}

The RISC-V specification defines a modular ISA with a base integer instruction set (RV32I) and optional extensions \cite{waterman2019riscv}. The M extension adds hardware multiply and divide instructions. Together, RV32IM provides a complete integer computation substrate suitable for embedded and educational processors. The fixed 32-bit instruction encoding and regular register-operand fields simplify decode logic and make the ISA well suited for pipelined implementation.

\subsection{Pipeline Depth Tradeoffs}

A pipelined processor overlaps execution of multiple instructions by partitioning the datapath into sequential stages separated by pipeline registers \cite{hennessy2017computer, patterson2020computer}. The classic RISC pipeline uses five stages: instruction fetch (IF), instruction decode and register read (ID), execute (EX), memory access (MEM), and write-back (WB) \cite{patterson1985reduced}. Data hazards are resolved through forwarding and stalling; control hazards are mitigated by branch prediction and pipeline flushing on misprediction.

Deeper pipelines reduce the combinational logic between pipeline registers, potentially increasing clock frequency. However, each additional stage increases the branch misprediction penalty by one cycle, adds pipeline register area and power, and introduces additional forwarding paths \cite{hrishikesh2002optimal}. A less commonly discussed effect is the impact on branch prediction accuracy itself. In a deeper pipeline, the latency from branch execution (where the prediction is verified) back to the fetch stage (where the prediction is consumed) spans more cycles. During these cycles, the predictor operates on stale state. If two branches are closely spaced in the instruction stream, the second branch may be fetched and predicted before the first branch's outcome has updated the PHT. This effect occurs even when the predictor hardware remains unchanged across pipeline variants; it is a consequence of the pipeline's update latency \cite{gonzalez1999power}.

\subsection{FPGA Architecture Considerations}

Xilinx 7-series FPGAs implement combinational logic in 6-input LUTs grouped into configurable logic blocks (CLBs) \cite{xilinx_7series}. Unlike ASIC standard cells, LUT delay is largely independent of the function implemented: a 32-bit addition and a 6-input Boolean function have comparable propagation times through the LUT fabric. Routing delay, which depends on placement and the programmable interconnect, often exceeds LUT delay and is less predictable at design time \cite{boutros2020fpga}. DSP48E1 hard blocks provide registered multiply-accumulate with fixed pipeline latency. Block RAM (BRAM) offers single-cycle synchronous read with registered output. These fabric properties mean that splitting a combinational path across two pipeline stages may not yield the same frequency improvement as on an ASIC, since routing delay is not eliminated by register insertion.

\subsection{Related RISC-V Soft Cores}

Table~\ref{tab:prior} summarizes the experimental scope of prior RISC-V FPGA implementations relative to this work. SERV \cite{serv2019}, PicoRV32 \cite{picorv32}, VexRiscv \cite{vexriscv}, Ibex \cite{schiavone2017slow}, CVA6 \cite{zaruba2019ariane}, and SweRV EH1 \cite{swerv2019} each present a single pipeline organization optimized for a particular design goal. Frequency and area numbers reported by these projects are useful contextual references, but they cannot be compared directly because ISA support, predictor design, memory architecture, forwarding strategy, and Vivado version differ across all of them. Prior surveys have cataloged pipelining techniques across RISC processor families \cite{biglari2019pipelining} and documented the Rocket Chip generator's configurable pipeline \cite{asanovic2016rocket}, but neither isolates pipeline depth as a controlled variable.

The gap in the existing literature is not the absence of pipeline-depth variation per se, but the absence of a controlled experiment in which \emph{only} pipeline depth varies while all other microarchitectural variables are held constant on the same FPGA device. Without such control, performance differences between published cores cannot be attributed to pipeline depth rather than to the many other variables that differ simultaneously.

\begin{table}[t]
\centering
\caption{Comparison of experimental scope with prior RISC-V FPGA implementations. \checkmark\ = varied; -- = fixed or not applicable.}
\label{tab:prior}
\small
\begin{tabular}{@{}lcccccc@{}}
\toprule
 & \rotatebox{70}{Pipeline depth} & \rotatebox{70}{Predictor} & \rotatebox{70}{ISA subset} & \rotatebox{70}{Memory type} & \rotatebox{70}{Forwarding} & \rotatebox{70}{Controlled} \\
\midrule
SERV \cite{serv2019}         & --  & --  & -- & -- & -- & -- \\
PicoRV32 \cite{picorv32}     & --  & --  & -- & -- & -- & -- \\
VexRiscv \cite{vexriscv}     & \checkmark & \checkmark & \checkmark & -- & \checkmark & -- \\
Ibex \cite{schiavone2017slow}& --  & --  & -- & -- & -- & -- \\
CVA6 \cite{zaruba2019ariane} & --  & --  & -- & -- & -- & -- \\
SweRV \cite{swerv2019}       & --  & --  & -- & -- & -- & -- \\
Rocket \cite{asanovic2016rocket} & \checkmark & \checkmark & \checkmark & \checkmark & \checkmark & -- \\
\textbf{This work}           & \checkmark & -- & -- & \checkmark & -- & \checkmark \\
\bottomrule
\end{tabular}
\vspace{2pt}

\footnotesize{``Controlled'' = only the indicated variable changes; all others are held constant from shared RTL.}
\end{table}

% ====================================================================
\section{Processor Design}
% ====================================================================

\subsection{Common Microarchitecture}

All five pipeline variants share the following modules, instantiated from identical RTL:

\textbf{ALU.} A combinational 10-operation arithmetic logic unit supporting ADD, SUB, AND, OR, XOR, SLT, SLTU, SLL, SRL, and SRA.

\textbf{Multiply-Divide Unit (MDU).} A 2-cycle pipelined multiplier with registered input and output stages, designed to infer DSP48E1 blocks. The divider uses a 32-cycle restoring algorithm. Supported operations are MUL, MULH, MULHSU, MULHU, DIV, DIVU, REM, and REMU. Division by zero and signed overflow follow the RISC-V specification \cite{waterman2019riscv}.

\textbf{Branch Predictor.} A gshare predictor \cite{mcfarling1993combining} with a 64-entry pattern history table (PHT) of 2-bit saturating counters, a 6-bit global history register (GHR), a 32-entry direct-mapped branch target buffer (BTB), and a 4-entry return address stack (RAS). The PHT is indexed by XORing the lower PC bits with the GHR. The predictor hardware is identical in all five variants. However, the number of pipeline stages between the fetch stage (where predictions are made) and the execute stage (where predictions are resolved and the PHT is updated) differs across variants. This update latency is the central variable in our analysis.

\textbf{CSR Unit.} Machine-mode control and status registers including \texttt{mstatus}, \texttt{mtvec}, \texttt{mepc}, \texttt{mcause}, \texttt{mscratch}, and 64-bit hardware performance counters for cycles (\texttt{mcycle}), retired instructions (\texttt{minstret}), total branches, and branch mispredictions. The performance counters are read by each benchmark to obtain cycle counts, instruction counts, branch counts, and misprediction counts.

\textbf{Instruction Memory.} A 1024-word (4\,KB) ROM initialized at elaboration via \texttt{\$readmemh}. A 64-line direct-mapped instruction cache with combinational hit logic sits between the PC and the instruction memory.

\textbf{Data Memory.} A 1024-word (4\,KB) synchronous RAM supporting byte, halfword, and word loads (signed and unsigned) and stores.

\textbf{Register File.} A 32$\times$32-bit register file with two read ports and one write port, including a write-through bypass for simultaneous read and write to the same register.

\subsection{Four-Stage Pipeline}

The 4-stage pipeline merges the memory access and write-back phases into a single stage: IF, ID, EX, MEM/WB. In this organization, the datapath performs load/store operations and register write-back in the same clock cycle. This reduces the total pipeline register count and eliminates load-use hazards: because loaded data is written back in the same stage it is read from memory, no stall is needed for a load followed by a dependent instruction.

Forwarding uses a single source: MEM/WB-to-EX. Branch mispredictions resolved in EX incur a 2-cycle penalty, flushing IF and ID. The 4-stage design has the simplest forwarding and hazard logic of the five variants but the longest combinational path, since the EX stage must contain the full ALU, branch comparator, and forwarding multiplexers, and the MEM/WB stage must perform both memory access and register write-back in one cycle.

\subsection{Five-Stage Pipeline}

The 5-stage pipeline follows the classic RISC organization: IF, ID, EX, MEM, WB. In the single EX stage, operand forwarding, ALU computation, branch condition evaluation, MDU operation, and CSR access all occur within one clock cycle. This creates a long combinational path comparable to the 4-stage design.

Forwarding uses two sources: MEM-to-EX and WB-to-EX. Load-use hazards (a load in MEM followed by a dependent instruction in EX) cause a one-cycle stall. Branch mispredictions resolved in EX incur a 2-cycle penalty, flushing IF and ID.

\subsection{Six-Stage Pipeline}

The 6-stage pipeline splits the execute phase into EX1 (operand forwarding and selection) and EX2 (ALU execution, branch resolution, MDU operation, and CSR access). This partitioning shortens the critical path by separating the forwarding multiplexer chain from the ALU and branch comparator.

Forwarding uses three sources: EX2-to-EX1, MEM-to-EX1, and WB-to-EX1. Load-use hazards cause a one-cycle stall. Branch mispredictions resolved in EX2 incur a 3-cycle penalty, flushing IF, ID, and EX1.

\subsection{Seven-Stage Pipeline}

The 7-stage pipeline adds a second fetch stage by splitting IF into IF1 (PC generation and instruction cache read) and IF2 (branch prediction using the registered PC and instruction). The stages are IF1, IF2, ID, EX1, EX2, MEM, WB.

The forwarding topology matches the 6-stage design: three sources from EX2, MEM, and WB to EX1. The deeper front end introduces two complications. First, when IF2 predicts a taken branch, the instruction already fetched by IF1 in the same cycle must be squashed, incurring a 1-cycle bubble on every correctly predicted taken branch. Second, branch mispredictions resolved in EX2 require flushing four stages (IF1, IF2, ID, EX1), yielding a 4-cycle penalty.

\subsection{Eight-Stage Pipeline}

The 8-stage pipeline adds a second memory stage by splitting MEM into MEM1 (address computation and memory read initiation) and MEM2 (memory read completion and data alignment). The stages are IF1, IF2, ID, EX1, EX2, MEM1, MEM2, WB.

Forwarding uses three sources: EX2-to-EX1 (for ALU results), MEM1-to-EX1 (for ALU results only; load data is not yet available), and MEM2-to-EX1 (for both ALU results and loaded data). This creates a dual load-use hazard: a load instruction in either EX2 or MEM1 requires a stall if the immediately following instruction depends on the loaded value, because load data is not available until MEM2. Branch mispredictions are resolved in EX2 with a 4-cycle penalty (same as the 7-stage), but the additional MEM1/MEM2 load-use stall cycles increase the effective CPI on load-heavy code.

\subsection{Hazard Handling Comparison}

Table~\ref{tab:hazard} summarizes the hazard characteristics of the five variants. The branch penalty increases from 2 to 4 cycles across the depth range. The 7- and 8-stage variants share the same branch penalty because both resolve branches in EX2 with the same number of preceding stages. The 8-stage design incurs additional CPI overhead from its dual load-use stall.

\begin{table}[t]
\centering
\caption{Hazard handling comparison across pipeline depths.}
\label{tab:hazard}
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Property} & \textbf{4} & \textbf{5} & \textbf{6} & \textbf{7} & \textbf{8} \\
\midrule
Pipeline registers        & 3 & 4 & 5 & 6 & 7 \\
Forwarding sources        & 1 & 2 & 3 & 3 & 3 \\
Load-use stall            & 0 & 1\,cy & 1\,cy & 1\,cy & 1--2\,cy \\
Branch penalty            & 2\,cy & 2\,cy & 3\,cy & 4\,cy & 4\,cy \\
IF2 prediction bubble     & -- & -- & -- & 1\,cy & 1\,cy \\
Predictor update latency  & 2 & 2 & 3 & 4 & 4 \\
\bottomrule
\end{tabular}
\end{table}

% ====================================================================
\section{Implementation and Verification}
% ====================================================================

All five variants are described in synthesizable SystemVerilog and target the Xilinx Artix-7 XC7A35TCPG236-1 device (Digilent Basys~3 board). Synthesis and implementation use Vivado 2025.2 in out-of-context mode to eliminate I/O buffer effects and isolate core logic timing. The clock constraint is set to 5\,ns (200\,MHz) to drive the tools toward maximum optimization effort; the reported $F_{\max}$ is derived from the worst negative slack (WNS) as $F_{\max} = 1000/(5 - \text{WNS})$.

\subsection{Experimental Invariants}
\label{sec:invariants}

The following variables are held constant across all five pipeline variants to ensure that measured performance differences are attributable to pipeline depth alone:
\begin{itemize}
\item \textbf{Shared RTL:} ALU, MDU, control decoder, branch predictor, CSR unit, register file, immediate generator, PC register, instruction cache, instruction memory, and data memory are instantiated from identical SystemVerilog source files in a common directory.
\item \textbf{ISA subset:} RV32IM with M-mode CSRs. No compressed (C) or floating-point extensions.
\item \textbf{Predictor hardware:} 64-entry gshare PHT, 32-entry BTB, 4-entry RAS. The predictor's internal state machines and update logic are identical; only the pipeline stage at which prediction occurs and the number of stages between prediction and resolution differ.
\item \textbf{Memory system:} 1024-word instruction ROM, 64-line direct-mapped instruction cache, 1024-word data RAM. All parameters identical.
\item \textbf{Tool flow:} Vivado 2025.2, out-of-context synthesis, xc7a35tcpg236-1 target, 5\,ns clock constraint. Each variant is synthesized with three different placement directives (Default, Explore, NoTimingRelaxation) to obtain frequency variance across placement seeds.
\end{itemize}
Each variant adds only three files: its top-level pipeline interconnect, forwarding unit, and hazard detection unit. All other RTL is shared at the file level, not merely architecturally equivalent.

\subsection{Multi-Seed Synthesis}

To quantify the placement-dependent variance in $F_{\max}$, each variant is synthesized and implemented three times with different placement and routing directives: (1) Default/Default, (2) Explore/Explore, and (3) Default/NoTimingRelaxation. This produces 15 independent implementation runs (5 variants $\times$ 3 seeds). The reported $F_{\max}$ values are means across the three seeds, with standard deviations providing error bars. This approach accounts for the sensitivity of FPGA timing closure to placement quality, which is especially relevant when comparing variants whose frequency differences are small relative to placement noise.

\subsection{Functional Verification}

Each variant is tested against 35 simulation-based test cases in Vivado's XSim simulator, covering integer arithmetic, M-extension multiply and divide, CSR read-modify-write sequences, trap handling, data-hazard forwarding stress tests, and multiply-to-add forwarding chains. All five variants pass all tests identically.

\subsection{Benchmark Workloads}

To measure CPI and branch prediction behavior, we run benchmark workloads on each pipeline variant using cycle-accurate simulation. Each benchmark reads the hardware performance counters (\texttt{mcycle}, \texttt{minstret}, branch count, misprediction count) before and after the workload, computes deltas, and stores the results to data memory for testbench readout. Functional correctness across pipeline depths is verified by comparing final memory checksums: matching checksums confirm identical computational results despite differing cycle counts.

\textbf{Dhrystone 2.1.} A widely used synthetic integer benchmark \cite{hennessy2017computer} ported to bare-metal RV32IM. The benchmark runs 100 iterations of the standard Dhrystone loop, executing 2{,}926 instructions with 400 branches (13.7\% branch frequency). All five pipeline variants produce identical instruction counts, branch counts, misprediction counts (203), and checksums.

\textbf{Diagnostic workload.} A mixed program containing integer arithmetic, load/store sequences, function calls with nested returns, and conditional branches. It exercises all major instruction categories, executing 1{,}367 instructions with 360 branches (26.3\% branch frequency). All five variants produce identical counts and checksums.

\textbf{Official EEMBC CoreMark.} The industry-standard CoreMark benchmark \cite{coremark}, compiled from the official EEMBC source (github.com/eembc/coremark) using riscv-none-elf-gcc~15.2.0 with -O1 optimization. The benchmark runs 10 iterations in PERFORMANCE\_RUN mode, executing 2{,}893{,}145 instructions with 719{,}810 branches (24.9\% branch frequency). A bare-metal porting layer provides timing via \texttt{mcycle} CSR reads and stores results to data memory for testbench readout. All five pipeline variants produce identical instruction counts, branch counts, and checksums.

\textbf{Embench-IoT: aha-mont64.} Montgomery multiplication from the official Embench-IoT suite (github.com/embench/embench-iot). This cryptographic kernel is multiply-heavy, executing 4{,}453{,}858 instructions with 512{,}593 branches (11.5\%). All five variants produce identical counts.

\textbf{Embench-IoT: crc32.} CRC-32 computation from Embench-IoT. Executes 4{,}008{,}648 instructions with 174{,}591 branches (4.4\%). The inner loop has extremely predictable branching (0.19\% misprediction rate). All five variants produce identical counts.

\textbf{Embench-IoT: statemate.} Finite state machine simulation from Embench-IoT. Executes 3{,}160{,}225 instructions with 376{,}291 branches (11.9\%). The data-dependent state transitions produce a moderate misprediction rate that increases with pipeline depth. All five variants produce identical counts.

\textbf{Embench-IoT: edn.} Encoder-decoder signal processing from Embench-IoT. Executes 2{,}972{,}527 instructions with 338{,}986 branches (11.4\%). All five variants produce identical counts.

All workloads are validated by confirming that every pipeline variant executes the same number of instructions and branches. The Embench benchmarks additionally include built-in verification functions that confirm correct computed results.

% ====================================================================
\section{Results}
% ====================================================================

\subsection{Synthesis Results}
\label{sec:synth}

Table~\ref{tab:results} presents the post-place-and-route results for all five variants, showing means and standard deviations from three placement seeds. Fig.~\ref{fig:fmax} shows the frequency comparison with error bars.

\begin{table}[t]
\centering
\caption{Post-place-and-route synthesis results on Artix-7 XC7A35T (mean $\pm$ std from 3 placement seeds).}
\label{tab:results}
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Metric} & \textbf{4-stg} & \textbf{5-stg} & \textbf{6-stg} & \textbf{7-stg} & \textbf{8-stg} \\
\midrule
$F_{\max}$ (MHz)  & 75.2  & 75.8  & 117.2  & 116.3  & 120.3 \\
$\pm$ std         & 1.8   & 1.7   & 1.5    & 2.3    & 3.8 \\
Slice LUTs        & 6{,}605 & 6{,}798 & 7{,}027 & 7{,}070 & 7{,}129 \\
Slice Regs (FFs)  & 7{,}956 & 8{,}124 & 8{,}413 & 8{,}489 & 8{,}625 \\
DSP48E1           & 12    & 12     & 12     & 12     & 12 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fmax_comparison.png}
\caption{Maximum clock frequency ($F_{\max}$) for each pipeline variant. Error bars show standard deviation from three placement seeds.}
\label{fig:fmax}
\end{figure}

The synthesis results show a two-tier frequency structure. The 4- and 5-stage designs achieve nearly identical frequencies (75.2 $\pm$ 1.8 and 75.8 $\pm$ 1.7\,MHz, respectively), while the 6-, 7-, and 8-stage designs cluster in the 116--120\,MHz range. The 6-stage achieves 117.2 $\pm$ 1.5\,MHz, a 56\% improvement over the 4-stage. The 7-stage achieves 116.3 $\pm$ 2.3\,MHz, and the 8-stage achieves 120.3 $\pm$ 3.8\,MHz.

A note on statistical confidence: with only three placement seeds per variant, the reported standard deviations should be interpreted as indicators of placement sensitivity rather than as tight confidence intervals. The 8-stage's higher mean $F_{\max}$ (120.3\,MHz) compared to the 6-stage (117.2\,MHz) amounts to a difference of 3.1\,MHz, which is smaller than the 8-stage's own standard deviation ($\pm$3.8\,MHz). A two-sample $t$-test on these three-point samples yields $p > 0.3$, confirming that the frequency difference between the 6-, 7-, and 8-stage variants is not statistically significant at conventional thresholds. The frequency gap between the shallow tier (4/5-stage, $\sim$75\,MHz) and the deep tier (6/7/8-stage, $\sim$117\,MHz) is unambiguous---the means differ by more than $20\times$ their standard deviations. Additional placement seeds would narrow the confidence intervals within each tier but would not change the tier structure itself.

The frequency jump from 5 to 6 stages is explained by the execute-stage split. In the 4- and 5-stage designs, the critical path runs through the forwarding multiplexer, ALU, and branch comparator in a single EX stage. Splitting this into EX1 (forwarding) and EX2 (computation) approximately halves the critical-path combinational depth, producing the observed 54--56\% frequency improvement. Further pipeline stages beyond 6 do not yield additional frequency gains because the EX1/EX2 split already addresses the dominant timing bottleneck. On FPGA fabric, where routing delay is a significant fraction of total path delay and is not eliminated by register insertion \cite{boutros2020fpga}, additional pipeline registers provide diminishing returns.

\subsection{Area}

Fig.~\ref{fig:area} shows the LUT and flip-flop utilization. The 4-stage uses the fewest resources (6{,}605 LUTs, 7{,}956 FFs). Each additional pipeline stage adds 43--193 LUTs and 65--289 FFs, with the total increase from 4 to 8 stages being 524 LUTs (7.9\%) and 669 FFs (8.4\%). All five variants use 12 DSP48E1 blocks for the pipelined multiplier, and no BRAM tiles since both instruction and data memories are implemented in distributed LUT RAM.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{area_comparison.png}
\caption{LUT and flip-flop utilization across five pipeline depths.}
\label{fig:area}
\end{figure}

\subsection{Benchmark Results: CPI}
\label{sec:cpi}

Table~\ref{tab:benchmark} presents the benchmark results for the two workloads validated across all five pipeline depths. Fig.~\ref{fig:cpi} compares CPI values.

\begin{table}[t]
\centering
\caption{Benchmark results across five pipeline depths. All variants execute identical instruction counts, branch counts, and misprediction counts for each workload.}
\label{tab:benchmark}
\begin{tabular}{@{}llccccc@{}}
\toprule
\textbf{Workload} & \textbf{Metric} & \textbf{4-stg} & \textbf{5-stg} & \textbf{6-stg} & \textbf{7-stg} & \textbf{8-stg} \\
\midrule
\multirow{5}{*}{\shortstack[l]{Dhrystone\\2.1}}
  & Cycles         & 4{,}134  & 4{,}334  & 4{,}938  & 5{,}440  & 5{,}840 \\
  & Instructions   & \multicolumn{5}{c}{2{,}926} \\
  & Branches       & \multicolumn{5}{c}{400} \\
  & Mispredictions & \multicolumn{5}{c}{203 (50.8\%)} \\
  & CPI            & 1.41     & 1.48     & 1.68     & 1.85     & 1.99 \\
\midrule
\multirow{5}{*}{Diagnostic}
  & Cycles         & 1{,}887  & 2{,}187  & 2{,}267  & 2{,}579  & 2{,}889 \\
  & Instructions   & \multicolumn{5}{c}{1{,}367} \\
  & Branches       & \multicolumn{5}{c}{360} \\
  & Mispredictions & \multicolumn{5}{c}{69 (19.2\%)} \\
  & CPI            & 1.38     & 1.60     & 1.66     & 1.89     & 2.11 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{cpi_comparison.png}
\caption{CPI across five pipeline depths and seven workloads. All benchmarks show monotonically increasing CPI with depth.}
\label{fig:cpi}
\end{figure}

CPI increases monotonically with pipeline depth for both workloads. The average CPI increase per additional stage is 0.145 for Dhrystone and 0.182 for Diagnostic. Both workloads produce \emph{identical} misprediction counts across all five depths: 203 out of 400 branches for Dhrystone (50.8\%) and 69 out of 360 for Diagnostic (19.2\%). These workloads' branch patterns are sufficiently spaced in the instruction stream that predictor update latency does not cause additional mispredictions, even at the deepest pipeline. The CPI increase is not solely attributable to branch misprediction penalty differences, either. The 4- and 5-stage designs both have a 2-cycle branch penalty, yet the 5-stage CPI is 0.07 higher on Dhrystone and 0.22 higher on Diagnostic. The latter difference reflects the 5-stage's 1-cycle load-use stall, which the 4-stage avoids due to its merged MEM/WB stage.

\subsubsection{Official CoreMark Results}

Table~\ref{tab:coremark} shows the official EEMBC CoreMark benchmark results across all five pipeline depths. The benchmark was compiled from the official EEMBC source code (github.com/eembc/coremark) using riscv-none-elf-gcc~15.2.0 with -O1 optimization for 10 iterations in PERFORMANCE\_RUN mode. All five variants execute identical instruction counts (2{,}893{,}145) and branch counts (719{,}810), confirming functional correctness across all pipeline depths.

Unlike Dhrystone and Diagnostic, CoreMark exhibits a depth-dependent increase in misprediction count. The 6-stage pipeline produces 154{,}077 mispredictions (21.4\%) versus 145{,}735 (20.2\%) for the 4- and 5-stage, a 5.7\% increase caused by the longer predictor update latency. The 7- and 8-stage variants show 147{,}760 mispredictions (20.5\%), between the shallow and deep extremes, reflecting the interaction between update latency and the IF2 prediction suppression during pipeline stalls.

\begin{table}[t]
\centering
\caption{Official EEMBC CoreMark results across five pipeline depths (10 iterations, PERFORMANCE\_RUN).}
\label{tab:coremark}
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Metric} & \textbf{4-stg} & \textbf{5-stg} & \textbf{6-stg} & \textbf{7-stg} & \textbf{8-stg} \\
\midrule
Cycles         & 4{,}481{,}534  & 4{,}724{,}937  & 5{,}310{,}372  & 5{,}856{,}929  & 6{,}323{,}069 \\
Instructions   & \multicolumn{5}{c}{2{,}893{,}145} \\
Branches       & \multicolumn{5}{c}{719{,}810 (24.9\%)} \\
Mispredictions & 145{,}735 & 145{,}735 & 154{,}077 & 147{,}760 & 147{,}760 \\
Mispredict rate & 20.2\%   & 20.2\%    & 21.4\%    & 20.5\%    & 20.5\% \\
CPI            & 1.54      & 1.63      & 1.83      & 2.02      & 2.18 \\
CoreMark/MHz   & 2.23      & 2.12      & 1.88      & 1.71      & 1.58 \\
\bottomrule
\end{tabular}
\end{table}

The CoreMark/MHz score decreases monotonically with pipeline depth (2.23 to 1.58) because the CPI increase outweighs any frequency advantage. This metric is frequency-independent: CoreMark/MHz = iterations $\times$ 10$^6$ / total\_cycles.

\subsubsection{Embench-IoT Results}

Table~\ref{tab:embench} presents four official Embench-IoT benchmarks validated across all five depths. Each benchmark was compiled from the official Embench-IoT source (github.com/embench/embench-iot) with our bare-metal porting layer. All four produce identical instruction and branch counts across pipeline depths, confirming functional correctness.

\begin{table}[t]
\centering
\caption{Embench-IoT benchmark CPI across five pipeline depths. All depths execute identical instruction counts for each benchmark.}
\label{tab:embench}
\small
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Benchmark ($f_b$)} & \textbf{4-stg} & \textbf{5-stg} & \textbf{6-stg} & \textbf{7-stg} & \textbf{8-stg} \\
\midrule
aha-mont64 (11.5\%) & 1.21 & 1.21 & 1.25 & 1.32 & 1.32 \\
crc32 (4.4\%)       & 1.30 & 1.30 & 1.39 & 1.52 & 1.56 \\
statemate (11.9\%)  & 1.21 & 1.30 & 1.37 & 1.50 & 1.69 \\
edn (11.4\%)        & 1.48 & 1.51 & 1.66 & 1.78 & 1.95 \\
\bottomrule
\end{tabular}
\end{table}

The Embench results show workload-dependent trends. The multiply-heavy aha-mont64 has the lowest CPI (1.21--1.32) because its long multiply sequences have few branches and benefit from the MDU pipeline. The crc32 benchmark shows almost zero mispredictions (344 across all depths, 0.19\%) because its inner loop is highly predictable. The statemate benchmark (FSM simulation) shows the strongest depth-dependent misprediction effect: 66{,}603 mispredictions at 4/5-stage vs.\ 73{,}263 at 7/8-stage (10.0\% increase), driven by its data-dependent state transitions. The edn benchmark has moderate CPI (1.48--1.95) with very low misprediction rates (3.1\%).

\subsection{Branch Misprediction Analysis}

Fig.~\ref{fig:mispred} compares misprediction rates for CoreMark and statemate---the two benchmarks that exhibit depth-dependent misprediction increases---across all five pipeline depths. On CoreMark, the 6-stage pipeline shows a 5.7\% increase in misprediction count relative to the 4- and 5-stage (154{,}077 vs.\ 145{,}735). On statemate, the effect is stronger: the 7- and 8-stage pipelines show 10.0\% more mispredictions than the 4- and 5-stage (73{,}263 vs.\ 66{,}603).

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{mispred_comparison.png}
\caption{Branch misprediction rates on CoreMark and statemate across five pipeline depths. Both benchmarks show depth-dependent misprediction increases, with statemate exhibiting a 10\% increase from 4-stage to 7/8-stage despite identical predictor hardware.}
\label{fig:mispred}
\end{figure}

Across all seven workloads, the misprediction-count increase requires two conditions simultaneously: (1) branches spaced closely enough that a new branch is fetched before the previous one's outcome updates the PHT, and (2) branch patterns that are sensitive to stale global history. Dhrystone, aha-mont64, crc32, and edn show zero misprediction increase across depths because their branch patterns are either widely spaced or highly predictable. CoreMark and statemate, with denser and more data-dependent branching, show the effect.

\subsection{Analytical CPI Model}
\label{sec:model}

To provide a quantitative design heuristic, we fit a linear model across all 35 validated data points (7 workloads $\times$ 5 pipeline depths) simultaneously:
\begin{equation}
\text{CPI} = 0.572 + 0.120 \cdot D + 1.903 \cdot f_b
\label{eq:cpi_model}
\end{equation}
where $D$ is the pipeline depth in stages (4 through 8) and $f_b$ is the workload's branch frequency (fraction of instructions that are branches). This model achieves $R^2 = 0.67$ with 32 degrees of freedom, a mean absolute error of 0.127\,CPI, and a maximum residual of 0.43\,CPI. Fig.~\ref{fig:cpi_vs_depth} shows the data points and fitted model lines.

We present this model as a design \emph{heuristic} rather than a predictive law. The $R^2$ of 0.67 means roughly one-third of the CPI variance is driven by workload-specific factors---base instruction mix, MDU stall frequency, load-use hazard density---that two parameters cannot capture. The model's value is directional: it correctly predicts that CPI rises with depth and with branch density, and the coefficients give order-of-magnitude guidance for design tradeoffs. For precise CPI prediction on a specific workload, cycle-accurate simulation remains necessary.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{cpi_vs_depth.png}
\caption{CPI vs.\ pipeline depth for all seven workloads, with fitted model lines ($R^2 = 0.67$, 35 observations).}
\label{fig:cpi_vs_depth}
\end{figure}

The coefficient of $D$ (0.120) represents the average CPI cost per additional pipeline stage across all workloads: each stage added beyond the 4-stage baseline costs approximately 0.12 CPI. The coefficient of $f_b$ (1.903) captures the strong CPI sensitivity to branch density: workloads with 10\% higher branch frequency pay approximately 0.19 more CPI at any given depth.

We compared four candidate models using the Akaike Information Criterion (AIC): (1) linear (Eq.~\ref{eq:cpi_model}, AIC~$= -124.6$), (2) interaction ($\alpha + \beta_1 D + \beta_2 D \cdot f_b$, AIC~$= -128.2$), (3) quadratic ($\alpha + \beta_1 D + \beta_2 D^2 + \beta_3 f_b$, AIC~$= -123.1$), and (4) full ($\alpha + \beta_1 D + \beta_2 f_b + \beta_3 D \cdot f_b$, AIC~$= -126.7$). The interaction model achieves the best AIC, suggesting that the CPI cost of pipeline depth is amplified by branch frequency. The 33\% of unexplained variance is attributable to workload-specific factors such as base CPI (which varies from 1.21 for multiply-heavy aha-mont64 to 1.48 for data-processing edn at the same pipeline depth), load-use hazard frequency, and MDU stall patterns that the two-parameter model does not capture.

\subsection{CPI Overhead Analysis}

Fig.~\ref{fig:sensitivity} shows the CPI overhead relative to the 4-stage baseline for four representative workloads. On CoreMark, the overhead increases from 5.8\% (5-stage) to 18.8\% (6-stage) to 31.2\% (7-stage) to 41.6\% (8-stage). Statemate shows the steepest progression: 7.4\%, 13.2\%, 24.0\%, and 39.7\%. The variation across workloads reflects differences in branch density and load-use hazard frequency.

The 8-stage design pays the largest CPI overhead despite sharing the same 4-cycle branch penalty as the 7-stage. On statemate, the 8-stage overhead (39.7\%) is 15.7 percentage points above the 7-stage (24.0\%). This additional cost arises from the dual load-use stall: loads require data from MEM2, but dependent instructions in EX1 need the data one stage earlier than in the 7-stage design.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{sensitivity_spacing.png}
\caption{CPI overhead relative to the 4-stage baseline for four representative workloads.}
\label{fig:sensitivity}
\end{figure}

\subsection{Throughput}

Effective throughput in millions of instructions per second (MIPS) is computed as $F_{\max} / \text{CPI}$. Fig.~\ref{fig:throughput} compares throughput using CoreMark CPI values. The 6-stage design achieves the highest throughput: 64.1\,MIPS on CoreMark and 70.6\,MIPS on Diagnostic. The 6-stage's 56\% frequency advantage over the 4- and 5-stage designs more than compensates for its higher CPI. The 7-stage achieves 57.6\,MIPS (CoreMark) and 61.5\,MIPS (Diagnostic), approximately 10\% below the 6-stage. The 8-stage achieves 55.2 and 57.0\,MIPS respectively, confirming that additional pipeline stages beyond 6 degrade net throughput on this fabric.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{throughput_comparison.png}
\caption{Throughput (MIPS) comparison using post-place-and-route $F_{\max}$ and CoreMark CPI.}
\label{fig:throughput}
\end{figure}

The 4-stage design achieves 48.8\,MIPS (CoreMark) despite its low frequency, because its low CPI partially compensates for the 75\,MHz clock. The 5-stage achieves the lowest throughput (46.5\,MIPS) because it has essentially the same frequency as the 4-stage but higher CPI due to load-use stalls.

Fig.~\ref{fig:coremark_mhz} shows the official CoreMark/MHz scores. The 4-stage achieves the highest per-MHz efficiency (2.23 CoreMark/MHz), decreasing monotonically to 1.58 for the 8-stage. These scores are directly comparable with published RISC-V cores: PicoRV32 achieves $\sim$0.5 CoreMark/MHz, Ibex $\sim$2.5, and VexRiscv $\sim$1.6--2.4.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{coremark_mhz.png}
\caption{Official EEMBC CoreMark/MHz scores across five pipeline depths.}
\label{fig:coremark_mhz}
\end{figure}

\subsection{Predictor Configuration Sensitivity}

To verify that the CPI overhead from deeper pipelining is not an artifact of a specific predictor configuration, we swept the PHT depth across 32, 64, and 128 entries while running Dhrystone on the 5-, 6-, and 7-stage variants (9 combinations total). Fig.~\ref{fig:predsweep} shows that neither CPI nor misprediction rate changes across any of the 9 configurations. All three PHT sizes produce identical results: 203 mispredictions out of 400 branches (50.8\%) on every variant.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{predictor_sweep.png}
\caption{CPI and misprediction rate on Dhrystone across three PHT sizes (32, 64, 128) and three pipeline depths. All 9 configurations yield identical misprediction counts, indicating that the CPI overhead is driven by pipeline depth rather than predictor sizing.}
\label{fig:predsweep}
\end{figure}

This result indicates that the CPI penalty from deeper pipelining is driven by increased branch resolution latency rather than by the predictor's table capacity. Dhrystone's 400 branches fit comfortably within even the 32-entry PHT, and the gshare XOR indexing distributes entries effectively at all three sizes.

To further establish generality across predictor \emph{architectures}, we replaced the gshare predictor with a bimodal predictor that indexes the PHT using only the PC (no global history register). Fig.~\ref{fig:predarch} compares the two architectures on Dhrystone. The bimodal predictor produces nearly identical CPI values (1.48, 1.68, 1.85 vs.\ 1.48, 1.68, 1.85 for gshare) and misprediction counts (202 vs.\ 203). The CPI trend across pipeline depths is preserved regardless of whether the predictor uses global history correlation or purely local PC-based prediction. This cross-architecture invariance strengthens the conclusion that the CPI overhead is a structural property of pipeline depth rather than a predictor-specific artifact.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{predictor_architecture.png}
\caption{CPI comparison between gshare and bimodal predictors on Dhrystone. The pipeline-depth CPI trend is preserved across predictor architectures.}
\label{fig:predarch}
\end{figure}

\subsection{Power and Efficiency}

Power estimates are available from Vivado's activity-based analysis for the 5-, 6-, and 7-stage variants. Fig.~\ref{fig:power} shows the on-chip power breakdown. Total power is nearly identical across all three variants: 0.299\,W (5-stage), 0.298\,W (6-stage), and 0.300\,W (7-stage). Static power is constant at 0.069\,W, as expected since it depends on device characteristics rather than design activity. Dynamic power is also nearly identical ($\sim$0.230\,W), indicating that the pipeline register overhead of deeper variants is offset by shorter combinational paths that reduce glitching \cite{chandrakasan1992low}. The practical implication is that pipeline depth has negligible impact on power for this class of design, making frequency and CPI the dominant factors in efficiency.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{power_comparison.png}
\caption{On-chip power breakdown (dynamic and static) for the 5-, 6-, and 7-stage variants. Power is approximately constant across pipeline depths.}
\label{fig:power}
\end{figure}

Fig.~\ref{fig:efficiency} shows the power efficiency in MIPS per watt, computed using the diagnostic workload throughput and estimated power. The 6-stage design leads at 237\,MIPS/W, followed by the 7-stage at 205\,MIPS/W and the 5-stage at 159\,MIPS/W. Since power is approximately constant across variants, efficiency tracks throughput directly, reinforcing the 6-stage as the most efficient design point.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{efficiency_comparison.png}
\caption{Power efficiency (MIPS/W) for the 5-, 6-, and 7-stage variants using diagnostic workload throughput.}
\label{fig:efficiency}
\end{figure}

\subsection{Effect of BRAM Instruction Memory}

To test whether the 7-stage IF split becomes beneficial with synchronous memory, we synthesized BRAM variants of the 5-, 6-, and 7-stage pipelines using \texttt{imem\_bram} (registered output, infers Block RAM) in place of the combinational LUT-based instruction memory. Fig.~\ref{fig:bram} shows the results.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{bram_comparison.png}
\caption{$F_{\max}$ comparison between LUT-based and BRAM-based instruction memory. The 7-stage design gains 4.0\% with BRAM, closing the gap to the 6-stage.}
\label{fig:bram}
\end{figure}

With BRAM, the 7-stage design improves from 116.3\,MHz to 120.9\,MHz (+4.0\%), nearly matching the 6-stage (121.5\,MHz). The 6-stage is essentially unchanged (117.2 to 121.5\,MHz), and the 5-stage slightly decreases (75.8 to 73.7\,MHz) because the BRAM's registered output adds latency to the already-long execute-stage critical path.

This result confirms that the 7-stage IF split is architecturally motivated by synchronous memory timing. When the instruction memory has a 1-cycle read latency (as BRAM does), the IF1/IF2 boundary naturally absorbs that latency, and the 7-stage design becomes competitive with the 6-stage on frequency. All three BRAM variants use 1 Block RAM tile and fewer LUTs than their distributed-memory counterparts, since the 4\,KB instruction ROM is moved from LUT fabric to dedicated BRAM. However, the CPI overhead of the deeper pipeline remains unchanged, so the 7-stage would need workloads with very low branch density to match the 6-stage's throughput even with BRAM-competitive frequency.

\subsection{Fmax vs.\ Pipeline Depth}

Fig.~\ref{fig:fmax_vs_depth} shows the $F_{\max}$ trend across all five depths. The non-monotonic shape---flat from 4 to 5 stages, a sharp increase at 6 stages, and a plateau through 8 stages---is consistent with the critical-path analysis. The 4-to-5-stage transition adds a pipeline register (MEM/WB split) that does not shorten the dominant timing path through the EX stage. The 5-to-6-stage transition breaks this path, producing the frequency jump. Subsequent stages split paths that are not timing-critical, yielding no further frequency improvement.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fmax_vs_depth.png}
\caption{$F_{\max}$ vs.\ pipeline depth. Error bars show standard deviation from three placement seeds. The frequency jump from 5 to 6 stages corresponds to the execute-stage split.}
\label{fig:fmax_vs_depth}
\end{figure}

\subsection{Comparison with Published Cores}

Fig.~\ref{fig:published} places our five variants in context with published RISC-V soft cores on Artix-7. Our cores occupy the mid-range of reported frequencies. The area is larger than minimal cores such as PicoRV32 \cite{picorv32} and SERV \cite{serv2019} because our design includes a full gshare branch predictor, instruction cache, CSR performance counters, and M-extension support. The frequency gap relative to PicoRV32 (250\,MHz) and SERV (300\,MHz) is expected: those cores use multi-cycle or bit-serial execution, eliminating the forwarding and branch-resolution combinational paths that constrain pipelined designs. CVA6 \cite{zaruba2019ariane}, which implements the full RV64GC ISA with a 6-stage pipeline, operates at approximately 80\,MHz on Artix-7, comparable to our 4-stage variant despite its significantly richer feature set.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{published_cores_comparison.png}
\caption{$F_{\max}$ comparison with published RISC-V soft cores on Artix-7.}
\label{fig:published}
\end{figure}

% ====================================================================
\section{Discussion}
% ====================================================================

\subsection{Two-Tier Frequency Structure}

The dominant feature of the synthesis results is the sharp frequency transition at the 5-to-6-stage boundary. The 4- and 5-stage designs are frequency-limited by the same combinational path through the monolithic execute stage. The 6-, 7-, and 8-stage designs all break this path, producing similar frequencies despite different total depths. This two-tier structure suggests that on Artix-7, the critical question is not ``how deep should the pipeline be?'' but rather ``does the pipeline split the execute stage?'' Once the execute split is made, additional stages provide marginal frequency benefit on this fabric.

The 8-stage design shows a slightly higher mean $F_{\max}$ than the 6- and 7-stage (120.3 vs.\ 117.2 and 116.3\,MHz), but the difference is within placement variance ($\pm$3.8\,MHz for the 8-stage). Multi-seed synthesis is essential for distinguishing genuine architectural improvements from placement noise.

\subsection{CPI Linearity}

Across all 35 data points (7 workloads $\times$ 5 depths), each additional pipeline stage costs approximately 0.12 CPI on average. This per-stage cost is consistent across diverse workloads: Dhrystone shows 0.145 CPI per stage, Diagnostic 0.182, CoreMark 0.160, and edn 0.118. The consistency occurs because each additional stage introduces a roughly constant amount of additional stall and flush overhead per instruction, regardless of absolute depth.

The CPI model (Eq.~\ref{eq:cpi_model}) captures 67\% of the total variance. The remaining 33\% arises from workload-specific factors: aha-mont64 has a base CPI of 1.21 (multiply-heavy, few stalls) while edn has 1.48 (more load-use hazards) at the same 4-stage depth. The branch frequency coefficient (1.903) quantifies this: workloads with 10\% higher branch frequency pay approximately 0.19 more CPI at any given depth.

\subsection{Pipeline Depth and Branch Prediction Effectiveness}

Dhrystone, Diagnostic, aha-mont64, crc32, and edn all show identical misprediction counts across all five pipeline depths. On these workloads, branches are either spaced widely enough or follow patterns regular enough that the predictor update latency does not cause additional mispredictions.

CoreMark and statemate tell a different story. On CoreMark, the 6-stage pipeline produces 154{,}077 mispredictions versus 145{,}735 for the 4- and 5-stage (5.7\% increase), with identical predictor hardware. On statemate, the effect is stronger: the 7- and 8-stage pipelines show 73{,}263 mispredictions versus 66{,}603 for the 4- and 5-stage (10.0\% increase). In both cases, the 4- and 5-stage variants---which share the same 2-stage update latency---produce identical misprediction counts, providing within-experiment confirmation that the increase is caused by update latency rather than other factors.

The critical factor is the interaction between branch density and pattern predictability. CoreMark has one branch every 4.0 instructions with data-dependent outcomes (linked-list comparisons, matrix clipping, FSM transitions). Statemate has one branch every 8.4 instructions but highly data-dependent state transitions. Both generate branch patterns that the gshare predictor cannot track accurately with stale global history. Workloads like crc32 (one branch every 23 instructions) or aha-mont64 (one per 8.7 instructions with predictable loops) do not trigger this effect.

\subsection{Why Six Stages Is Optimal on This Fabric}

The 6-stage design achieves the best throughput because it sits at the knee of the frequency curve---capturing the full benefit of the execute-stage split---while incurring only moderate CPI overhead. Deeper pipelines (7, 8 stages) achieve similar or slightly higher frequencies but pay progressively higher CPI costs that outweigh any frequency advantage. Shallower pipelines (4, 5 stages) have better CPI but are frequency-limited by the monolithic execute stage. For designers, the practical implication is clear: on LUT-based FPGA fabric, the first pipeline split that matters is separating the forwarding multiplexer from the ALU. Splits beyond that point buy little frequency and cost measurable CPI.

The 4-stage design has the lowest CPI (1.38--1.41) thanks to its merged MEM/WB stage eliminating load-use stalls, but its 75\,MHz frequency limits throughput to 48--55\,MIPS. The 5-stage design fares worst in throughput: it adds load-use stalls (raising CPI) without gaining frequency. On this fabric, the classic 5-stage RISC pipeline is a dominated design point---worse than both the shallower 4-stage and the deeper 6-stage.

\subsection{Decomposition of the 7-Stage CPI Overhead}

The 7-stage CPI penalty relative to the 6-stage is larger than the branch penalty alone would predict. On the diagnostic workload, CPI increases from 1.66 (6-stage) to 1.89 (7-stage), a delta of 0.23\,CPI. Table~\ref{tab:hazard_cost} summarizes the per-event cycle costs across all five variants, providing the analytical basis for decomposing this delta.

\begin{table}[t]
\centering
\caption{Per-event cycle costs across pipeline variants. ``Update latency'' is the number of stages between prediction and resolution.}
\label{tab:hazard_cost}
\small
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Event} & \textbf{4} & \textbf{5} & \textbf{6} & \textbf{7} & \textbf{8} \\
\midrule
Branch mispredict flush    & 2\,cy & 2\,cy & 3\,cy & 4\,cy & 4\,cy \\
Load-use stall             & 0\,cy & 1\,cy & 1\,cy & 1\,cy & 1--2\,cy \\
IF2 taken-prediction bubble & --   & --    & --    & 1\,cy & 1\,cy \\
MDU multiply stall         & 2\,cy & 2\,cy & 2\,cy & 2\,cy & 2\,cy \\
MDU divide stall           & 32\,cy & 32\,cy & 32\,cy & 32\,cy & 32\,cy \\
Predictor update latency   & 2\,stg & 2\,stg & 3\,stg & 4\,stg & 4\,stg \\
\bottomrule
\end{tabular}
\end{table}

Using these costs, the 6-to-7-stage CPI delta decomposes into three sources:

\begin{enumerate}
\item \textbf{Longer misprediction penalty} (4 vs.\ 3 cycles). With 69 mispredictions over 1{,}367 instructions, the additional cycle per misprediction accounts for $69 / 1{,}367 = 0.050$\,CPI (22\% of the delta).
\item \textbf{IF2 prediction bubble.} When IF2 predicts a taken branch, IF1's speculative sequential fetch is discarded, costing 1 cycle per correctly predicted taken branch. With approximately 70\% of correctly predicted branches being taken, this accounts for an estimated 0.15\,CPI (65\% of the delta).
\item \textbf{Indirect depth effects}---increased forwarding latency and pipeline drain overhead on function calls---account for the remaining $\sim$0.03\,CPI (13\%).
\end{enumerate}

The IF2 bubble is thus the dominant cost of the 7-stage design. This cost is inherent to any two-stage fetch split: IF1 fetches sequentially while IF2 predicts, and a taken prediction always discards the speculative IF1 fetch. It cannot be eliminated without speculative fetch cancellation or a zero-cycle predictor. Even with BRAM-competitive frequency, this CPI overhead means the 7-stage would need workloads with very low branch density to match the 6-stage's throughput. The 8-stage design inherits this same IF2 bubble cost and adds the dual load-use stall overhead, explaining its even higher CPI despite sharing the 7-stage's 4-cycle branch penalty.

\subsection{FPGA vs.\ ASIC Pipeline Depth}

On ASIC processes, deeper pipelines generally increase $F_{\max}$ monotonically because gate delay scales with the amount of logic per stage \cite{hrishikesh2002optimal, sprangle2002increasing}. On FPGAs, LUT delay is relatively constant regardless of function complexity, and routing delay is a significant fraction of total path delay. Inserting a pipeline register does not eliminate routing delay on either side of the register, limiting the frequency benefit of deeper pipelining. The two-tier frequency structure observed in our data---flat below 6 stages, flat above---is a consequence of this FPGA-specific property. ASIC implementations would likely show a more gradual frequency increase across the same depth range.

\subsection{Applicability to Other FPGA Families}

This study targets Artix-7 exclusively. We identify three fabric properties that drive the observed tradeoffs: (1) LUT-based combinational logic with near-constant propagation delay, (2) routing-dominated critical paths where register insertion does not eliminate interconnect delay, and (3) synchronous BRAM with registered output requiring at least one cycle of read latency. These properties are shared by other Xilinx families (UltraScale, UltraScale+) and Intel FPGA devices (Cyclone, Stratix) \cite{kuon2007measuring}.

We \emph{hypothesize}---but have not validated---that the qualitative trends (two-tier frequency structure, monotonic CPI increase, predictor-latency misprediction effect) would appear on those platforms as well. The hypothesis rests on architectural reasoning, not empirical data from other fabrics. Quantitative differences are expected: UltraScale's faster routing fabric may shift the frequency tier boundary, and Intel's ALM-based logic may change the critical-path composition. The specific $F_{\max}$ values, area costs, and optimal pipeline depth reported here are Artix-7-specific and should not be extrapolated without device-specific validation.

The CPI model (Eq.~\ref{eq:cpi_model}) depends on pipeline depth and branch frequency, neither of which is fabric-dependent. The per-stage CPI cost of 0.12 should transfer across FPGA families because it is driven by pipeline hazard structure, not by timing. The absolute throughput ($F_{\max} / \text{CPI}$), however, is fabric-specific because $F_{\max}$ varies with the target device.

\subsection{Limitations}

This study has several limitations. First, the CPI model ($R^2 = 0.67$, 35 data points) explains only two-thirds of the variance. The remaining third arises from workload-specific base CPI differences that a two-parameter model cannot capture. A per-workload intercept model would fit better but would not generalize to new workloads. Second, all simulation results are deterministic single-run measurements; run-to-run variance is zero by construction for RTL simulation. The benchmark suite spans seven workloads from three standard sources (Dhrystone, EEMBC CoreMark, Embench-IoT) covering crypto, networking, signal processing, and control-flow patterns, but additional workloads (e.g., compiled application kernels, SPEC-like integer benchmarks) would further test generalizability. Third, the comparison is limited to a single FPGA family (Artix-7). Although the mechanisms that drive the two-tier frequency structure are present in other families \cite{kuon2007measuring}, empirical validation on UltraScale or Intel FPGAs is needed. Fourth, all workloads fit within 16\,KB instruction memory with near-100\% cache hit rates. At realistic application scales where caches genuinely miss, memory stall cycles would dilute the relative contribution of branch misprediction penalties to CPI. Fifth, power estimates from Vivado's activity-based analysis show approximate parity ($\sim$0.30\,W) across pipeline depths, but these estimates have a typical accuracy range of 15--30\% and differences within this margin should not be treated as precise rankings.

% ====================================================================
\section{Conclusion}
% ====================================================================

This work presents a controlled comparison of 4- through 8-stage RV32IM pipelined processors on a Xilinx Artix-7 FPGA, with all non-pipeline microarchitectural variables held constant. Multi-seed post-place-and-route synthesis shows a two-tier frequency structure: the 4- and 5-stage designs cluster near 75\,MHz, while the 6-, 7-, and 8-stage designs cluster near 117--120\,MHz. The frequency transition at the 5-to-6-stage boundary corresponds to the execute-stage split that breaks the dominant critical path.

CPI increases at approximately 0.12 per additional pipeline stage across seven workloads, with the per-stage cost consistent from multiply-heavy crypto (aha-mont64, 0.03/stage) through mixed-workload CoreMark (0.16/stage) to load-heavy signal processing (edn, 0.12/stage). On the official EEMBC CoreMark benchmark, the 4-stage processor achieves 2.23 CoreMark/MHz, decreasing to 1.58 at 8 stages. The 6-stage achieves the highest absolute throughput (64.1\,MIPS on CoreMark, 1.88 CoreMark/MHz) by capturing the full frequency benefit of the execute-stage split while paying only a moderate CPI penalty.

Pipeline depth degrades branch prediction effectiveness on two of seven benchmarks. On CoreMark, the 6-stage produces 5.7\% more mispredictions than the 4-stage despite identical predictor hardware. On the Embench statemate benchmark (FSM simulation), the 7/8-stage pipelines produce 10\% more mispredictions than the 4/5-stage. The remaining five workloads show zero misprediction increase, confirming that the effect requires both close branch spacing and data-dependent branch patterns. The CPI trend is invariant to predictor table capacity (PHT 32 to 128) and predictor architecture (gshare vs.\ bimodal).

The 4-stage design offers the lowest CPI but is frequency-limited to 75\,MHz. The classic 5-stage RISC pipeline is a dominated design point: it shares the 4-stage's frequency limitation while adding load-use stalls that increase CPI. The 7- and 8-stage designs achieve similar frequencies to the 6-stage but pay progressively higher CPI costs---the 7-stage's IF2 prediction bubble alone accounts for 65\% of its CPI delta relative to the 6-stage. BRAM-based instruction memory closes the frequency gap between the 7-stage and 6-stage (120.9 vs.\ 121.5\,MHz), validating the IF split for synchronous-memory architectures, but the CPI overhead remains.

Future work includes extending the comparison to UltraScale and Intel FPGA families, broadening the benchmark suite with compiled application kernels and additional Embench-IoT workloads, and investigating speculative predictor update mechanisms that could reduce effective update latency in deeper pipelines.

The complete RTL, testbenches, synthesis scripts, and benchmark programs are publicly available at \url{https://github.com/devanshjoshi08/RISCV-RV32IM-Processor}.

% ====================================================================
% References
% ====================================================================

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
